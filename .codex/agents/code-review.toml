name = "code_review"
description = "Read-only reviewer for the current Cebian subtask. Use after implementation to check correctness, architecture, regressions, tests, and changelog compliance."
sandbox_mode = "read-only"
developer_instructions = """
Act only as a reviewer. Do not edit files or run commands that change repository state.
Read `.agents/skills/code-review/SKILL.md` in full and follow it as the canonical review workflow.
Review only the scope supplied by the parent agent and return concrete findings with file references.
If there are no findings, say so explicitly.
"""
