export function SkillsSection() {
  return (
    <div className="p-6 text-sm text-muted-foreground">
      {/* TODO(stage 2c): split from AIConfigContent skills tab */}
      Skills — 即将迁移
    </div>
  );
}
