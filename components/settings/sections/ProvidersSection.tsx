export function ProvidersSection() {
  return (
    <div className="p-6 text-sm text-muted-foreground">
      {/* TODO(stage 2a): migrate content from ProviderManagerDialog */}
      Providers — 即将迁移
    </div>
  );
}
