export function InstructionsSection() {
  return (
    <div className="p-6 text-sm text-muted-foreground">
      {/* TODO(stage 2b): migrate from old SettingsPanel "自定义指引" */}
      指引 — 即将迁移
    </div>
  );
}
