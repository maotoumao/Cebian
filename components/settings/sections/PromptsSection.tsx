export function PromptsSection() {
  return (
    <div className="p-6 text-sm text-muted-foreground">
      {/* TODO(stage 2c): split from AIConfigContent prompts tab */}
      Prompts — 即将迁移
    </div>
  );
}
