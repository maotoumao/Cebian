# Cebian Development Rules

The project rules live in [AGENTS.md](../AGENTS.md) at the repository root — one file shared by every AI coding agent. **Read it before starting any task and follow it in full.**

Do not add rules here; edit `AGENTS.md` instead.
